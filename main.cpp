#include <iostream>

int main(){
    // std::cout is how C++ prints to the terminal
    // std::endl adds a line break at the end
    std::cout << "Hello World!" << std::endl;

    // Returning 0 tells your Mac's opertaing system that the program ran without crashing
    return 0;
}